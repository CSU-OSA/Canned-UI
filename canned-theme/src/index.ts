import { createTheme } from "@/api/createTheme";
import { currentTheme } from "@/api/currentTheme";
import { switchTheme } from "@/api/switchTheme";
import { useThemeManager } from "@/api/useThemeManager";

export { createTheme, currentTheme, switchTheme, useThemeManager };